# OpenMemory MCP Server — Environment Variables
# These can also be set in your MCP client config (e.g. claude mcp add --env)

MEMORY_API_URL=http://localhost:8765
MEMORY_USER_ID=steven
MEMORY_APP_NAME=claude-code
